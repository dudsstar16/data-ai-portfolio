{
  "name": "eduarda-reis-data-ai",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "node scripts/build.mjs",
    "check": "node scripts/check.mjs && npm run build",
    "sync:github": "node scripts/sync-github-projects.mjs",
    "sync:certificates": "node scripts/sync-certificates.mjs",
    "content:sync": "npm run sync:github && npm run sync:certificates"
  }
}
